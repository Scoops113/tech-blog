<!DOCTYPE html>
<html>
<head>
  <title>Home Page</title>
</head>
<body>
  
  {{#if user}}
    <p>Welcome, {{user.username}}!</p>
  {{else}}
    <p>Please log in.</p>
  {{/if}}

  <h1>Blog Posts</h1>
  {{#each posts}}
    <div>
      <h2>{{this.title}}</h2>
      <p>{{this.content}}</p>
      <p>Posted by {{this.username}} on {{this.createdAt}}</p>
    </div>
  {{/each}}
</body>
</html>
